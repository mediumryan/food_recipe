import styled from 'styled-components';

export const NavbarWrapper = styled.nav`
  ul {
    display: flex;
    li {
      padding: 0.5rem;
      margin: 0.25rem;
      a {
        font-size: 0.95rem;
        text-decoration: none;
      }
    }
  }
`;
